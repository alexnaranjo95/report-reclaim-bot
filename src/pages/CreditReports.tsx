import React from "react";

const CreditReportPage: React.FC = () => {
  return (
    <main className="container mx-auto px-6 py-10" data-testid="credit-report-empty">
      <h1 className="text-2xl font-semibold tracking-tight">Credit Report</h1>
      <p className="text-muted-foreground mt-2">
        This view is intentionally empty for a new implementation.
      </p>
    </main>
  );
};

export default CreditReportPage;
